---
name: aigc-emotion-director
description: |
  AIGC短片人物情绪与说话感染力优化技能。当用户需要优化AI视频中人物的情绪表现、面部微表情、说话语气感染力时使用此技能。覆盖Prompt情绪工程、逐秒面部拆解、情绪关键词体系、配音符号控制、防AI僵硬策略、FACS面部动作编码校验。适用场景包括：AI数字人说话没感情、AIGC短片角色表演生硬、需要设计情绪表演Prompt、优化TTS配音的情绪表达、需要更精确控制面部动作时序。触发词："人物情绪""感染力""说话没感情""AI角色表演""表情Prompt""配音情绪""微表情"、"AIGC情绪"。
---

# AIGC Emotion Director — 人物情绪与说话感染力优化

## Purpose

本技能将研究文档中的方法论转化为可直接使用的工作流程。**核心能力：接收用户的一段完整提示词，分析其中人物的情绪和说话语气，拆解对应的面部微表情，为台词嵌入配音符号，只修改情绪/台词/表情相关部分，输出纯文本优化版（不超过2000字）。** 未涉及台词的提示词同样适用——自动识别情绪并拆解表情，不强制添加声音描述。

## Core Workflow（六步法）

### Step 0：解析提示词

收到用户的完整提示词后，先做三件事：

1. **识别角色**：提取提示词中所有人物/角色的名称和身份描述。
2. **识别台词**：提取所有带引号的对白或独白，标注每个台词属于哪个角色。
3. **识别情绪线索**：从提示词中找出已有的情绪描述词（如"得意""虚弱""愤怒"等），记录原文位置以便后续替换。

### Step 1：情绪状态分析

根据提示词中的角色设定、场景上下文、台词以及已有情绪线索，**推断每个角色此刻“在经历什么”，而不只是给情绪贴标签**。参考以下框架：

- 结合 Seedance 2.0 的效价-唤醒（Valence-Arousal）二维情绪模型判断情绪基调。
- 判断是否存在情绪转折（如“先X后Y”“从A变为B”）。
- 判断人物是在释放、克制、压抑、伪装还是反向表达。该判断只作为内部导演决策，不强制原样输出。
- 如果台词与真实情绪可能不一致，内部判断是否存在潜台词；只有确实存在言行不一致时才启用，不要为了“高级”而强行脑补。
- 如果提示词没有明确指定情绪，根据场景、角色关系和动作线做最少必要推断，避免过度心理分析。

输出：每个说话角色（或所有表演角色）的「情绪定位」——一句话情绪基调 + 强度数值（X→Y分）+ 变化方式（慢提/迅速升/隐忍/安静保持/先压后炸）。

### Step 2：FACS辅助的面部微表情拆解

先用 **FACS（Facial Action Coding System）** 做一次内部动作校验，再把结果翻译回自然、可执行的中文表演语言。**FACS 是内部编码层，不是最终Prompt的展示语言。**

执行顺序：
1. 根据 Step 1 选出当前情绪段落真正起作用的 **2-5 个核心 Action Units（AU）**，不要为了完整而堆满整张脸。
2. 为关键 AU 判断激活强度（优先使用 A-C；只有明确需要强表演时再到 D-E），并判断其出现顺序、峰值和回落。
3. 找出 **1 个主导信号 + 1-2 个辅助信号 + 最多 1 个情绪泄漏点**；其余面部区域保持稳定或仅做自然联动。
4. 将 AU 结果转换成自然语言，例如“眉间轻收、下颌逐渐绷紧、嘴唇压住”，最终Prompt默认**不输出AU编号**，除非用户明确要求FACS编码。
5. 检查动作之间是否互相冲突，尤其是“微笑+压嘴”“说话口型+闭唇动作”等，避免同一时刻安排不可能同时发生的面部动作。

参照 `references/facs_action_units.md` 获取AU对照、强度和组合规律。参照 `references/emotion_keywords.md` 把编码转译为自然语言。

**重要原则：** FACS用于让表演动作更稳定、更具体，**不用于把AU直接等同于某种情绪**。同一AU组合可能服务于不同的心理状态，必须结合场景、台词、关系和时间线判断。

**面部五要素词库（执行层）：**

| 要素 | 高频词 |
|------|--------|
| 眼神/瞳孔 | 失去聚焦、放空、光暗下来、飘忽闪烁、先亮后活络、穿过镜头 |
| 眼睑/眼眶 | 下眼睑轻提、眼尾泛红、眼眶湿润、慢慢涨起、眼皮压低 |
| 眉峰 | 轻挑一侧、往中间皱、眉峰下压、耷拉无支撑、往上又收紧 |
| 嘴角/唇 | 扯出笑意、抿住、内收、向下垮、松弛、颤抖、咬紧 |
| 呼吸/声音 | 变轻变弱、沉下去、不稳起伏、哽住发紧、大段起伏、突然炸开 |

**核心原则**：永远用生理细节替代抽象情绪标签。不写“她很难过”，写“眼眶湿润，下眼睑涨起，呼吸短促后重新压平”。不写“她生气了”，写“眉间收紧，目光变窄，嘴唇压住，下颌逐渐绷紧”。

**不要平均分配五组面部要素**：不要求每一秒都同时改变眼神、眼睑、眉峰、嘴唇、呼吸。优先突出主导信号，再用少量辅助信号形成联动。这样能避免“AI五官一起演”的塑料感。

**时间维度**：情绪不是一瞬间，而是一个过程。描述“从A状态变化到B状态”的过程，而非“始终处于B状态”。如果提示词中的情绪描述包含了“先X后Y”的转折，务必在拆解时保留这个动态过程，并让关键AU在时间轴上具有合理的进入、峰值与回落。

### Step 3：台词逐句分析（仅当提示词包含台词时执行）

在逐句设计前，先检查上一阶段的FACS动作是否与发音口型、停顿、呼吸协调。**说话时不要安排会和口型直接冲突的闭唇/咬唇等动作，停顿与吞咽、吸气、抿唇等动作要有明确时间位置。**

对每一句台词，执行以下分析：

**a. 按词组拆解音高/音量/质感**：
- 音高：上扬（疑问/撒娇）vs 下沉（放弃/笃定）
- 音量：轻（隐忍/虚弱）vs 厚重（爆发/强调）
- 质感：带笑意 / 哽住发紧 / 撕裂发颤 / 沙哑 / 气声

**b. 嵌入配音符号**（参照 4.4 配音符号情绪对照表）：

| 符号 | 效果 | 适用场景 |
|------|------|----------|
| `……` | 声断气留，停顿0.5-2秒 | 犹豫、虚弱、思考、神秘留白 |
| `！` | 声带加压，气流爆破 | 兴奋、命令、爆发 |
| `~` | 音高滑动上扬，尾音拖长 | 撒娇、轻快、得意 |
| `——` | 前字拉长后骤停 | 声音中断、欲言又止、转折 |
| `**` | 字词短促断开 | 加重强调、压迫感 |
| `？！` | 先迟疑再爆发 | 震惊、催促、骄傲反问 |
| `……！` | 压抑后爆发 | 想忍但没忍住 |
| `！…` | 爆发后泄气 | 激动后瞬间低落 |
| `#` | 状态标签 | #whisper、#cry 等 |

选择原则：根据 Step 1 的情绪定位和 Step 3a 的拆解结果，为每句台词选择合适的符号。在台词中直接嵌入，并在台词后用括号标注符号含义。

**c. 应用七维度优化**（参照 6.6 章节）：

维度一：公共约束层——先锁定全片不变的部分（画幅、风格、机位、景别、背景、角色一致性、负面约束），情绪描述只替换表演段落。

维度二：时间切片法——把每个角色的情绪拆成连续的生理状态变化过程（初始→铺垫→临界→释放→余韵），拒绝"从第一秒就在目标情绪"。

维度三：生理细节替代情绪标签——提示词中所有抽象情绪词（开心、悲伤、愤怒、失望、得意等）全部替换为具体的面部肌肉/器官描述。

维度四：声音描述——每句台词标注音高走向、音量变化、声音质感，确保"不看字幕也能感知情绪"。

维度五：防翻车约束——在每个情绪段落的末尾设置否定约束，告诉模型不要做什么。每条约束对应 AI 最容易犯的一个错误（如"不要夸张表情"→AI 默认倾向过度表演）。

维度六：情绪强度数值化——用 X→Y分 + 变化方式标签替代模糊的情绪描述。

维度七：可迁移模板——输出格式统一为：台词（带符号）+ 语气拆解（围绕符号展开）+ 禁止项。

### Step 4：集成输出

**关键约束：只修改台词、情绪和表情相关的描述，提示词中其他部分（场景设定、画质、镜头描述、非表演类内容）严格保持原样。**

将优化后的内容整合：

1. 保留原提示词中不变的部分（基础设定、氛围画质、非对话镜头描述等）。
2. 将原提示词中涉及情绪/表演/台词的描述替换为 Step 1-3 优化后的版本。
3. 确保每个有情绪的段落包含：情绪强度数值 + 核心生理细节 + 声音描述（如有台词）+ 防翻车约束。只保留真正参与当前表演的面部动作，不为了凑齐五组要素而堆描述。
4. 如有台词，确保台词以符号嵌入版呈现。
5. 输出纯文本，总字数不超过 2000 字。

### Step 5：自检

输出前过一遍检查清单：
1. 所有抽象情绪词都尽可能转换成可观察的生理细节了吗？
2. 每个情绪段落标注了 X→Y分 + 变化方式吗？
3. 每个台词都嵌入了合适的配音符号并附了简短说明吗？
4. FACS内部动作是否形成了连贯组合，是否存在互相冲突或“五官一起动”的问题？
5. 每个情绪段落末尾有至少 2 条针对当前风险的否定约束吗？
6. 声音描述覆盖了音高+音量+质感三参数吗？
7. 是否只修改了情绪/台词/表情相关部分，未改镜头/场景/画质等？
8. 总字数在 2000 字以内吗？

## Quality Standards

生成的优化版 Prompt 须满足：
- 最终文本以自然、可执行的表演语言为主，不输出大段心理分析或FACS编码表，除非用户明确要求
- 抽象情绪词只作为定位辅助，最终应尽可能落到可观察的生理动作
- 情绪强度用数值标注（X分→Y分）+ 变化方式标签
- 台词附带配音符号嵌入版 + 符号含义说明
- 每个有角色的段落有至少两条针对当前风险的负面约束
- 面部动作有主次、有时序、有联动，避免所有五官同时变化
- FACS只作为内部动作编码与一致性校验层，不强制直接暴露给视频模型
- 只修改情绪/台词/表情部分，其他内容保持不变
- 纯文本输出，不超过 2000 字

## Reference Files

- `references/prompt_templates.md` — 四层模板 + 完整可迁移示例 + 6版"滚"的案例 + 七维度方法论
- `references/emotion_keywords.md` — 面部五要素高频词库 + 情绪关键词分级体系
- `references/facs_action_units.md` — FACS核心Action Units、强度等级、常用组合与AI视频转译规则
- `references/voice_symbols.md` — 20种配音符号对照表 + 使用示例
- `references/directing_tips.md` — 镜头语言情绪对照 + 导演风格锚定词 + 音画协同
